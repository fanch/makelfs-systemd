# Description: The Wget package contains a utility useful for non-interactive downloading of files from the Web.
# URL: http://www.gnu.org/software/wget/
# Maintainer: NuTyX core team
# Packager: thierryn1 at hispeed dot ch

source=(http://ftp.gnu.org/gnu/$name/$name-$version.tar.xz 
	http://www.linuxfromscratch.org/patches/blfs/svn/wget-$version-texi2pod-1.patch)

build() {
cd $name-$version
patch -Np1 -i ../wget-$version-texi2pod-1.patch 
./configure --prefix=/usr \
--bindir=/bin \
--sysconfdir=/etc \
--with-ssl=openssl \
--disable-debug \
--mandir=/usr/share/man \
--infodir=/usr/share/info
make
make DESTDIR=$PKG install
rm -rf $PKG/usr/share/info/dir
}
