# Description: Librairie posix 1003.1e version 2
# URL: http://www.kernel.org/pub/linux/libs/security/linux-privs/
# Maintainer: NuTyX core team
# Packager: thierryn1 at hispeed dot ch
# Depends on: attr
# Run on: attr

source=( http://ftp.de.debian.org/debian/pool/main/libc/$name/${name}_$version.orig.tar.gz)

build() {
cd libcap-$version
make
make RAISE_SETFCAP=no prefix=/usr DESTDIR=$PKG LIBDIR=$PKG/usr/lib install

chmod -v 0755 $PKG/usr/lib/libcap.so.2.22

mkdir $PKG/lib 
mv -v $PKG/usr/lib/libcap.so.* $PKG/lib
ln -sfv ../../lib/libcap.so.2 $PKG/usr/lib/libcap.so
}
